export enum Routes {
  AUTH = 'auth',
  USERS = 'users',
  POSTS = 'posts',
  CONVERSATIONS = 'conversations',
}

export enum Services {
  AUTH = 'AUTH_SERVICE',
  USERS = 'USERS_SERVICE',
  CONVERSATIONS = 'CONVERSATIONS_SERVICE',
  POSTS = 'POSTS_SERVICE',
}
